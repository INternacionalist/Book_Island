<Window x:Class="WpfAppBookStore.SuccessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Успех" Height="260" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F2F6FF" Topmost="True" WindowStyle="None" AllowsTransparency="True">
    <Border Background="White" CornerRadius="18" BorderBrush="#DCE7FF" BorderThickness="1.5" Padding="22">
        <StackPanel>
            <Border Width="56" Height="56" CornerRadius="28" Background="#E8EEFF" HorizontalAlignment="Center">
                <TextBlock Text="✓" FontSize="26" FontWeight="Bold" Foreground="#3553D8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="Готово" FontSize="24" FontWeight="Bold" Foreground="#182033" HorizontalAlignment="Center" Margin="0,14,0,8"/>
            <TextBlock x:Name="MessageText" HorizontalAlignment="Center" FontSize="14" Foreground="#65708A" TextAlignment="Center" TextWrapping="Wrap" Margin="4,0,4,22"/>

            <Button Content="Отлично" Height="40" Width="140" HorizontalAlignment="Center" Background="#4B6BFB" Foreground="White" FontWeight="SemiBold" Click="OkBtn_Click" Cursor="Hand" BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</Window>
